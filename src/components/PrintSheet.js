import React from 'react';

import firebase, {firestore} from '../firebase'

import {Grid, TextField, Button, Typography, Table} from '@material-ui/core'

export default class PrintSheet extends React.Component {
    constructor(props) {
        super(props);
        this.state = {

        }   
    }

    render(){
        return(
            <React.Fragment>

            </React.Fragment>
        );
    }
};
